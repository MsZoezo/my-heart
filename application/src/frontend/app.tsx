import "./app.css";

import React from 'react';
import { createRoot } from 'react-dom/client';

const root = createRoot(document.body);

root.render(
    <React.StrictMode>
        <h1>Hello world!</h1>
    </React.StrictMode>
);